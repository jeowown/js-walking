function repeatWord(str, n) {
    for (let i = 0; i < n; i++) {
        console.log(str);
    }
  }
  
  // 함수 실행
(repeatWord("hello", 5));
  
// `repeatWord` 함수의 내용을 채워주세요.
//  “hello”, 5를 넣어 `repeatWord`  함수를 실행시켜주세요.